import { ExpdirectiveDirective } from './expdirective.directive';

describe('ExpdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
